import {Link} from 'react-router-dom';

export default function Navbar(){
    return (
        <div className="ui inverted fixed small menu">
            <a className="item">
                <span className='text-primary fs-2'>R</span>A  
            </a>
        </div>
    );
}